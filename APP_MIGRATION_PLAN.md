# PinQ Android 앱 전환 계획

## 현재 상태

- React 웹 UI를 지도 전체 화면, 플로팅 검색·필터, 3단 바텀 시트로 개편했다.
- `Capacitor` Android 프로젝트를 만들고 React `build/`를 앱 자산으로 동기화할 수 있게 했다.
- 웹과 Android는 같은 코스 규칙 엔진을 사용한다. 서버가 연결되면 LLM은 검증된 후보의 순위만 정하고, 연결되지 않으면 규칙 기반 코스를 만든다.
- Android APK/AAB 실행, 실제 Google Places 조회, 실제 LLM 호출은 아직 검증되지 않았다.
- 기본 서비스 지역은 서울 용산구다. 2026-07-01 행정동 경계 안에서 출발점과 장소를 제한하고 최대 48개 장소를 표시한다. 실제 Google 응답의 완전성은 보장되지 않는다.

## 단계별 작업

### 1. 개발용 Android 앱 실행

1. Android Studio, Android SDK, JDK와 에뮬레이터 또는 실기기를 준비한다. [Capacitor Android 안내](https://capacitorjs.com/docs/android)
2. `npm ci`, `npm run android:sync`로 웹 파일을 앱에 복사한다.
3. `npm run android:open`으로 Android Studio에서 프로젝트를 열고 에뮬레이터·실기기에서 실행한다.
4. 작은 화면, 다크 모드, 키보드 노출, 뒤로가기, 시트 드래그, 긴 장소명, 오프라인에서 조작을 확인한다.

### 2. 실제 지도와 장소 검색 연결

- 웹뷰에서 Google Maps JavaScript API와 Places API (New)가 동작하는지 확인한다. 현재 `.env`에 지도 키가 없으면 예시 지도가 열린다.
- Android 배포 키는 앱 패키지·서명 인증서 기반 제한이 가능한 네이티브 지도 연동을 우선 검토한다. 웹뷰용 키를 사용할 경우 앱 출처에서의 API 키 제한 방식과 실제 기기 동작을 먼저 확인한다.
- 지도와 바텀 시트 높이 변화 시 마커·경로가 가리지 않는지 실기기에서 검증한다.
- 현 구현의 직선 연결선·추정 이동시간을 실제 길찾기로 대체할지 결정한다.

### 3. 추천 API와 AI 운영 연결

- `REACT_APP_RECOMMEND_API_URL`에 운영용 HTTPS 추천 API 주소를 빌드 시 지정한다. Android 앱에는 CRA 개발 프록시가 없다.
- 현재 서버는 `127.0.0.1:3001` 개발용이며 인증이 없다. 운영 배포 전 인증, 요청량 제한, CORS 출처 제한, 모니터링을 추가한다.
- LLM 키는 서버에만 두고 앱에 포함하지 않는다. 서버 응답 실패 때의 규칙 기반 대체 동작을 기기에서 확인한다.
- API 후보와 클라이언트 후보가 같은 예산·거리·시간 조건을 만족하는지 회귀 테스트를 늘린다.

### 4. 제품 데이터 정확도

- 실제 장소의 영업시간·특별 휴무·수용 인원·메뉴 가격은 현재 검증되지 않는다. 이용자에게 추정/미확인 상태를 분명히 표시한다.
- `로컬 N%`는 가상 데이터에만 표시한다. 실제 소상공인 여부를 표시하려면 검증 가능한 데이터 공급원과 갱신 정책이 필요하다.
- 야간 이동, 막차, 조명·보행 안전 판단은 실제 경로 데이터 없이는 확정 표현을 피한다.

### 5. 배포 준비

- 첨부 PNG는 화면 로고로 적용했다. 앱 아이콘과 스플래시는 정사각형 고해상도 원본으로 별도 제작하고 Android 리소스를 교체한다.
- `com.pinq.app`은 임시 앱 ID이므로 Play 등록 전에 확정한다.
- 공개 저장소 이력에 있던 Google Maps/LLM 키는 폐기·재발급하고 새 키를 제한한다. 키를 앱 저장소나 React 번들에 비밀값으로 넣지 않는다.
- 오래된 Create React App 의존성의 보안 경고를 해결하기 위해 빌드 도구 마이그레이션을 계획한다.
- Play Console 등록, 개인정보 처리 고지, 앱 서명, 비공개 테스트, 심사 정보를 준비한다.

## 추천 엔진 확인 결과

`src/engine.mjs`가 장소 3개 조합의 거리·추정 이동시간·체류·예산을 계산한다. 데모 데이터에 대해서만 고정 영업시간과 정원을 검증한다. `server/index.mjs`는 클라이언트가 보낸 가격·로컬 여부를 신뢰하지 않고 다시 계산한다. LLM은 최대 3개 후보의 순위를 제시할 수 있지만, 서버가 후보 ID의 개수·중복·허용 여부를 검증하고 실패 시 규칙 순위로 돌아간다. 이는 장소를 자유 생성하거나 실제 길찾기를 수행하는 AI가 아니다.

## 이번 구현 검증 기준

- `npm test`: 규칙·서버 테스트 8개 통과
- `npm run build`: React 프로덕션 빌드 성공
- 브라우저 모바일 화면: 코스 3개, 지도 마커, 비용 분해, 다크 모드, 시트 전환 확인
- Android: `cap add android` 및 웹 자산 동기화 성공. 기기 실행은 SDK·에뮬레이터 환경에서 후속 확인 필요
